import React from 'react';
import { IonContent } from '@ionic/react';

const Profile = () => {
    return (
        <IonContent>
            <div>Profile</div>
        </IonContent>
    );
}

export default Profile;